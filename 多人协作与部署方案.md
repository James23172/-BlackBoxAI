# 多人协作与部署方案

## 一、设计原则

```
主公机 = 基础设施 + 调度中枢（轻量，不参与计算）
客户端 = 全部计算分布在 4 台电脑上
所有模块通过局域网连接主公机的 Redis + RabbitMQ
```

---

## 二、五台电脑模块分工

```
┌───────────────────────────────────────────────────────────────┐
│  主公机（基础设施 + 调度中枢 + 主大屏）                           │
│                                                               │
│  Redis :6379      RabbitMQ :5672                              │
│  AuthServer :8890  Controller（全局唯一调度）                    │
│  Display :8888（投屏演示）                                      │
│  Navigator×1（可选）  Car×1（可选）                              │
│                                                               │
│  轻量级模块，不参与计算密集型任务                                  │
└───┬─────────┬─────────┬─────────┬─────────┬───────────────────┘
    │         │         │         │         │
    ▼         ▼         ▼         ▼         ▼
┌────────┐┌────────┐┌────────┐┌────────┐┌────────┐
│ 客户端 B ││ 客户端 C ││ 客户端 D ││ 客户端 E ││ 主公机  │
│TaskCfg  ││TargetP  ││        ││        ││Nav×1   │
│Nav×1    ││Nav×1    ││Nav×1   ││Nav×1   ││Car×1   │
│Car×2    ││Car×1    ││Car×2   ││Car×1   ││Display │
│Display  ││Display  ││Display ││Display ││:8888   │
│:8889    ││:8890    ││:8891   ││:8892   ││        │
└────────┘└────────┘└────────┘└────────┘└────────┘
```

### 模块分配总表

| 模块 | 主公机 | B | C | D | E | 可多实例 |
|------|:-----:|:-:|:-:|:-:|:-:|:--------:|
| Redis | ✅ | — | — | — | — | ❌ |
| RabbitMQ | ✅ | — | — | — | — | ❌ |
| AuthServer | ✅ | — | — | — | — | ❌ |
| Controller | ✅ | — | — | — | — | ❌ |
| Display | ✅:8888 | ✅:8889 | ✅:8890 | ✅:8891 | ✅:8892 | ✅ |
| TaskConfigurator | — | ✅ | — | — | — | ❌ |
| TargetPlanner | — | — | ✅ | — | — | ❌ |
| Navigator | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Car | ✅×1 | ✅×2 | ✅×1 | ✅×2 | ✅×1 | ✅ |

### 为什么 AuthServer 和 Controller 留在主公机

| 模块 | 留在主公机的原因 |
|------|-----------------|
| AuthServer | 极轻量（HTTP 简单转发），前端硬编码 `localhost:8890` |
| Controller | 调度大脑，单实例。管理员直接控制主公机，最可靠 |

---

## 三、每台电脑看到的内容

| 电脑 | 浏览器打开 | 可登录角色 | 画面 |
|------|----------|-----------|------|
| 主公机 | `localhost:8888` | 配置员 / 运行员 / 分析员 | 主大屏，接投影仪演示 |
| B | `localhost:8889` | 运行员 / 分析员 | 同画面 + 自己跑 TaskCfg + 2 辆车 |
| C | `localhost:8890` | 运行员 / 分析员 | 同画面 + 自己跑 TargetPlanner + 1 辆车 |
| D | `localhost:8891` | 运行员 / 分析员 | 同画面 + 自己跑 2 辆车 |
| E | `localhost:8892` | 运行员 / 分析员 | 同画面 + 自己跑 1 辆车 |

> 配置员仅限主公机登录。运行员和分析员可在任意主机登录。
> 所有 Display 通过 RabbitMQ Fanout Exchange 接收同一份 `REFRESH_ALL`，画面毫秒级同步。

---

## 四、三级用户角色

| 角色 | 登录范围 | 功能 |
|------|---------|------|
| **系统配置员** | 仅主公机 | 查看画面、登录注册加密、权限表加密、地图配置、障碍物编辑、用户管理、**全局开始/暂停/重置**（最高权限） |
| **运行人员** | 所有主机（含主公机） | 查看画面、添加小车、**控制自己小车的开始/暂停**（受配置员管控） |
| **系统分析员** | 所有主机（含主公机） | 查看画面、回放历史 tick、性能分析（无控制权限） |

### 权限层级

```
              ┌──────────────┐
              │  系统配置员    │  ← 最高权限，主公机独占
              │ 全局开始/暂停  │
              │ 配置员暂停后   │
              │ → 运行员无法启动│
              └──────┬───────┘
                     │ 管控
              ┌──────▼───────┐
              │  运行人员      │  ← 所有主机登录
              │ 只能控制自己的车 │
              │ 配置员暂停=全局停│
              │ → 自己的车也停  │
              └──────────────┘
                     │
              ┌──────▼───────┐
              │  系统分析员    │  ← 所有主机登录
              │ 只读，无控制权 │
              └──────────────┘
```

### 开始/暂停逻辑

```
配置员 点【开始】
  → Controller 全局激活 → 所有小车开始移动
  → 5 台电脑画面同步

配置员 点【暂停】
  → Controller 全局暂停 → 所有小车停止
  → 运行员的【开始】按钮变灰，不可点击
  → 原因: 配置员权限高于运行员

运行员 点【暂停】(自己控制的小车)
  → 仅该运行员的 Car 暂停
  → 其他运行员的小车不受影响

运行员 点【开始】(自己控制的小车)
  → 仅该运行员的 Car 恢复移动
  → 前提: 配置员未全局暂停
  → 配置员全局暂停时，按钮变灰不可用
```

### 登录鉴权流程

```
用户打开浏览器 → 输入 localhost:端口
  │
  ├─ 登录页 → 输入用户名+密码+选择角色
  ├─ 发送到 AuthServer (主公机 :8890)
  │
  ├─ 系统配置员登录
  │     ├─ 非主公机浏览器 → "配置员仅限主公机登录"
  │     └─ 主公机浏览器 → 进入全功能控制台
  │         （密码 SHA-256+盐哈希存储，权限表 AES 加密）
  │
  ├─ 运行人员登录
  │     └─ 任意主机浏览器 → 进入运行界面
  │         （地图实时画面 + 添加小车按钮）
  │
  └─ 系统分析员登录
        └─ 任意主机浏览器 → 进入分析界面
            （回放面板 + 探索率曲线 + 步数统计）
```

---

## 五、页面设计

### 通用布局结构

```
┌─────────────────────────────────────────────────────────────┐
│  👤 用户名（角色）                            [退出登录]      │  ← 顶栏
├──────────────────────┬──────────────────────────────────────┤
│                      │                                      │
│      地图画布        │         右侧面板（按角色不同）         │
│     (Canvas)        │                                      │
│    40×40 格         │                                      │
│                      │                                      │
│   ██ 障碍物           │                                      │
│   ░░ 已探索           │                                      │
│   ·· 未探索           │                                      │
│   🚗 小车位置         │                                      │
│                      │                                      │
├──────────────────────┴──────────────────────────────────────┤
│  状态栏: 探索率 45.2% | 小车总数 6 | 已用步数 2048            │  ← 底栏
└─────────────────────────────────────────────────────────────┘
```

---

### 5.1 系统配置员页面（仅主公机 :8888 登录）

```
┌─────────────────────────────────────────────────────────────┐
│  👤 admin（系统配置员）  [主公机]               [退出登录]     │
├──────────────────────┬──────────────────────────────────────┤
│                      │  📐 地图参数                          │
│                      │  ┌────────────────────────────┐      │
│                      │  │ 宽度: [40]  高度: [40]      │      │
│                      │  │ 障碍物密度: [0.15]         │      │
│                      │  │ 小车数量: [6]              │      │
│                      │  └────────────────────────────┘      │
│                      │                                      │
│      地图画布        │  🖱️ 障碍物编辑                        │
│  （实时同步）        │  ┌────────────────────────────┐      │
│                      │  │ ○ 随机生成  ○ 鼠标点击添加   │      │
│                      │  │ 点击地图格子 → 切换障碍物    │      │
│                      │  └────────────────────────────┘      │
│                      │                                      │
│                      │  👥 用户管理                          │
│                      │  ┌────────────────────────────┐      │
│                      │  │ 用户名: [______]           │      │
│                      │  │ 密码:   [______]           │      │
│                      │  │ 角色:   ○配置员 ○运行员 ○分析员│    │
│                      │  │ [创建用户]                  │      │
│                      │  │ ─────────────────────────  │      │
│                      │  │ admin      配置员  [删除]   │      │
│                      │  │ operator   运行员  [删除]   │      │
│                      │  └────────────────────────────┘      │
│                      │                                      │
│                      │  🎮 全局控制（最高权限）               │
│                      │  ┌────────────────────────────┐      │
│                      │  │ [全局开始] [全局暂停] [全局重置]│    │
│                      │  │ 配置员暂停 → 全局冻结         │      │
│                      │  └────────────────────────────┘      │
├──────────────────────┴──────────────────────────────────────┤
│  探索率 45.2% │ 小车 6 辆 │ 步数 2048 │ 状态: 运行中          │
└─────────────────────────────────────────────────────────────┘
```

| 区域 | 功能 | 说明 |
|------|------|------|
| 地图画布 | 实时显示 + 点击编辑障碍物 | 左键切换障碍物，所有 Display 同步 |
| 地图参数 | 宽/高/密度/车数 | 重置后生效 |
| 障碍物编辑 | 随机生成 / 鼠标点击 | 配置员专属 |
| 用户管理 | 创建/删除用户 | 仅配置员可见，权限表 AES 加密存储 |
| 全局控制 | 开始/暂停/重置 | 最高权限，影响所有运行员 |

---

### 5.2 运行人员页面（所有主机 :8888~8892 登录）

```
┌─────────────────────────────────────────────────────────────┐
│  👤 user1（运行人员）  [客户端B]               [退出登录]     │
├──────────────────────┬──────────────────────────────────────┤
│                      │  📊 我的小车                           │
│                      │  ┌────────────────────────────┐      │
│                      │  │ Car001  位置(12,8)  步数342  │      │
│                      │  │ Car002  位置(25,14) 步数298  │      │
│                      │  │                            │      │
│                      │  │ [暂停我的车]  [恢复我的车]    │      │
│                      │  │                            │      │
│                      │  │ 系统状态: ●运行中           │      │
│                      │  │ (配置员未全局暂停)          │      │
│                      │  └────────────────────────────┘      │
│      地图画布        │                                      │
│  （实时同步）        │  🚗 车队管理                          │
│                      │  ┌────────────────────────────┐      │
│                      │  │ [添加新车]  (+ 1 辆小车)     │      │
│                      │  └────────────────────────────┘      │
│                      │                                      │
│                      │  ⚠️ 权限说明                          │
│                      │  ┌────────────────────────────┐      │
│                      │  │ 配置员全局暂停时 → 按钮锁定  │      │
│                      │  │ 你只能控制自己的小车         │      │
│                      │  └────────────────────────────┘      │
├──────────────────────┴──────────────────────────────────────┤
│  探索率 45.2% │ 小车 6 辆 │ 步数 2048 │ 我的车: 2辆          │
└─────────────────────────────────────────────────────────────┘
```

| 区域 | 功能 | 说明 |
|------|------|------|
| 地图画布 | 实时显示（只读） | 与主公机同步 |
| 我的小车 | 查看自己控制的车 | 位置、步数、状态 |
| 控制按钮 | 暂停/恢复自己的车 | 配置员全局暂停时按钮变灰 |
| 车队管理 | 添加新车 | 新车自动分配 ID + 随机位置 |
| 权限说明 | 提示当前受谁管控 | 配置员暂停时显示红色警告 |

---

### 5.3 系统分析员页面（所有主机 :8888~8892 登录）

```
┌─────────────────────────────────────────────────────────────┐
│  👤 analyst1（系统分析员）  [客户端C]           [退出登录]    │
├──────────────────────┬──────────────────────────────────────┤
│                      │  ⏪ 回放面板                           │
│                      │  ┌────────────────────────────┐      │
│                      │  │ Tick 范围: [0] ~ [2048]     │      │
│                      │  │ 当前: 1280  速度: [1x]      │      │
│                      │  │                            │      │
│                      │  │ [⏮] [⏪] [⏸] [▶] [⏩] [⏭]  │      │
│                      │  │ 后退 减速  暂停 播放 加速  前进  │      │
│                      │  │                            │      │
│                      │  │ ████████░░░░ 进度条          │      │
│                      │  └────────────────────────────┘      │
│      地图画布        │                                      │
│  （实时 + 回放）     │  📈 性能分析                           │
│                      │  ┌────────────────────────────┐      │
│                      │  │ 探索率曲线                  │      │
│                      │  │ 100%│               ···    │      │
│                      │  │  50%│          ··          │      │
│                      │  │   0%│··                     │      │
│                      │  │    └──────────────────       │      │
│                      │  │      0s           120s      │      │
│                      │  │                            │      │
│                      │  │ 📊 统计                     │      │
│                      │  │ 总步数:  2048              │      │
│                      │  │ 总耗时:  120.5s            │      │
│                      │  │ 平均速度: 17.0步/秒         │      │
│                      │  │ 最终探索率: 95.8%           │      │
│                      │  │                            │      │
│                      │  │ 🚗 单车统计                 │      │
│                      │  │ Car001: 342步  覆盖率20.1% │      │
│                      │  │ Car002: 298步  覆盖率18.3% │      │
│                      │  │ Car003: 415步  覆盖率24.6% │      │
│                      │  └────────────────────────────┘      │
├──────────────────────┴──────────────────────────────────────┤
│  探索率 45.2% │ 模式: ○实时 ●回放(Tick 1280/2048)            │
└─────────────────────────────────────────────────────────────┘
```

| 区域 | 功能 | 说明 |
|------|------|------|
| 地图画布 | 实时模式 / 回放模式 | 回放时地图根据 tick 恢复到历史状态 |
| 回放面板 | 选择范围 + 控制按钮 | 暂停/播放/加速/减速/逐帧前进后退 |
| 进度条 | 可视化回放进度 | 拖动跳转到任意 tick |
| 探索率曲线 | 折线图 | X轴时间，Y轴探索率 |
| 统计面板 | 全局 + 单车统计 | 总步数、耗时、速度、覆盖率 |

---

### 5.4 底栏

| 指标 | 含义 | 更新频率 |
|------|------|---------|
| 探索率 | 已探索格 / 可探索格 × 100% | 每次 tick |
| 小车数量 | 当前活跃小车数 | 实时 |
| 步数 | 所有小车累计移动步数 | 每次 MOVE_STEP |
| 状态 | 运行中 / 已暂停 / 已完成 | 实时 |

---

## 六、加密方案

### 6.1 总览：四层加密体系

```
用户输入密码
  │
  ├─ ① 传输层: 前端 SHA-256(password) → 不可逆摘要 → 网络传输（无明文）
  │
  ├─ ② 存储层: 后端 SHA-256(salt + 传输层摘要) → salt:hash → 存 Redis
  │                                                (salt 随机 16 字节)
  │
  ├─ ③ 权限表: AES-128-ECB(JSON权限数据) → 密文字节 → 存 Redis
  │
  └─ ④ Token签名: HMAC-SHA256(username:role:timestamp, 服务端密钥)
                    → Base64 → 返回前端 → 每次请求带 Token
```

| 层 | 算法 | 存储位置 | 可逆 |
|------|------|----------|:--:|
| 传输层 | SHA-256 | 不存储（仅传输用） | ❌ |
| 存储层 | SHA-256 + 随机盐 | Redis `auth:user:{name}` | ❌ |
| 权限表 | AES-128-ECB | Redis `auth:permissions` | ✅ (服务端解密) |
| Token | HMAC-SHA256 | 仅存前端 localStorage | ❌ (防篡改) |

---

### 6.2 登录注册加密流程

```
┌─ 注册 ───────────────────────────────────────────────────┐
│                                                          │
│  前端                         AuthServer                  │
│  ┌──────────┐                ┌──────────────────────┐    │
│  │ password │                │                      │    │
│  │   ↓      │                │                      │    │
│  │ SHA-256  │  ──POST──→    │ 接收 transportHash    │    │
│  │ = transHash              │   ↓                   │    │
│  └──────────┘               │  hash = SHA-256(      │    │
│                              │    randomSalt(16B) +  │    │
│                              │    transHash)         │    │
│                              │   ↓                   │    │
│                              │  存储: "salt:hash"     │    │
│                              │  → Redis              │    │
│                              └──────────────────────┘    │
│                                                          │
└──────────────────────────────────────────────────────────┘

┌─ 登录 ───────────────────────────────────────────────────┐
│                                                          │
│  前端                         AuthServer                  │
│  ┌──────────┐                ┌──────────────────────┐    │
│  │ password │                │                      │    │
│  │   ↓      │                │                      │    │
│  │ SHA-256  │  ──POST──→    │ 从 Redis 读 "salt:hash"│   │
│  │ = transHash              │   ↓                   │    │
│  └──────────┘               │  SHA-256(salt +        │    │
│                              │    transHash)          │    │
│                              │   ↓                   │    │
│                              │  比较 Redis 中的 hash  │    │
│                              │  一致 → HMAC Token    │    │
│                              └──────────────────────┘    │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**为什么有两层 SHA-256**

| 只做一层 | 两层 |
|----------|------|
| 传输层明文 → 中间人抓包看到密码 | 前端先 SHA-256 → 抓包看到的是哈希值 |
| 数据库泄露 → hash 可直接撞库 | salt 不同 → 相同密码不同 hash，无法彩虹表 |

---

### 6.3 权限表加密（AES-128）

```
权限表结构（明文）:                 Redis 中存储（密文）:
{                                  ┌──────────────────┐
  "CONFIGURATOR": [                │ 0xA4 0x3F 0x88   │
    "START","PAUSE","RESET",       │ 0x12 0x9B 0xC7   │
    "SET_CONFIG","ADD_CAR",        │ 0x35 0xE2 0x01   │
    "TOGGLE_OBSTACLE",             │ ...               │
    "VIEW_ALL"                     │ (二进制密文)       │
  ],                               └──────────────────┘
  "OPERATOR": [
    "START","PAUSE","VIEW_ALL"             ↑
  ],                              AES-128-ECB 加密
  "ANALYST": [                    密钥 = "BlackBoxAI!2024!!"
    "VIEW_ALL",                    (16 字节硬编码)
    "REPLAY_PLAY",
    "REPLAY_SEEK"
  ]
}
```

**加密流程：**

```
启动 AuthServer
  │
  ├─ PermissionManager.savePermissions(defaultPermissions)
  │     ├─ fastjson2 序列化 Map<Role, List<String>> → JSON 字符串
  │     ├─ Cipher.getInstance("AES/ECB/PKCS5Padding")
  │     ├─ SecretKeySpec(KEY_16BYTES, "AES")
  │     ├─ cipher.doFinal(jsonBytes) → byte[]
  │     └─ jedis.set("auth:permissions".getBytes(), encryptedBytes)
  │
读取时（loadPermissions）:
  │
  ├─ jedis.get("auth:permissions") → byte[]
  ├─ cipher.doFinal(encrypted) → JSON 字符串
  └─ JSON.parseObject → Map<String, List<String>>
```

**为什么用 ECB 模式**

- 权限数据量极小（几百字节），ECB 的块重复问题不显著
- 实现简单，无 IV 管理开销
- 如需增强：改用 `AES/CBC/PKCS5Padding` + 随机 IV 存入 Redis 前半部分

---

### 6.4 Token 签名（HMAC-SHA256）

```
登录成功
  │
  ├─ 生成 Token:
  │     payload = username + ":" + role + ":" + timestamp
  │     signature = HMAC-SHA256(payload, SERVER_SECRET)
  │     token = Base64Url(payload + ":" + signature)
  │
  ├─ 返回前端: { "token": "eyJhbGc...", "username": "...", "role": "..." }
  │
  └─ 前端 localStorage.setItem('token', token)

验证请求:
  │
  ├─ 从 token 解码 → 提取 payload + signature
  ├─ 重新 HMAC-SHA256(payload, SERVER_SECRET)
  ├─ 比较签名
  │     ├─ 一致 → 信任 token 中的 username + role
  │     └─ 不一致 → 拒绝（token 被篡改）
  │
  └─ 不检查 exp（课程项目简化，如有需要可加时间戳校验）
```

**SERVER_SECRET：**

```java
// AuthServer 启动时随机生成，重启后失效（要求重新登录）
private static final String SECRET = generateSecret();
// SecureRandom 32 字节 → Base64 编码 → 内存持有，不落盘
```

---

### 6.5 安全威胁与防御

| 威胁 | 防御 |
|------|------|
| 密码明文传输被截获 | 前端 SHA-256 后传输，抓包只能看到 hash |
| Redis 被拖库 | 密码存 SHA-256(salt+hash)，彩虹表无效；权限表 AES 密文 |
| Token 被窃取 | HMAC 签名防篡改，窃取后无法伪造其他用户 Token |
| 暴力破解密码 | 限流（可加 Guava RateLimiter），连续 5 次失败锁 30 秒 |
| XSS 窃取 localStorage | 课程项目可接受，生产环境用 HttpOnly Cookie |
| 重放攻击 | 每次 Token 含 timestamp，可加 nonce 防重放（课程项目可省略） |
| 配置员越权登录 | 前端 `hostname !== 'localhost'` 拦截 + 后端 IP 校验 |

---

## 七、页面路由与角色识别

```
浏览器打开 localhost:端口
  │
  ├─ 读取 localStorage.token
  │
  ├─ token 存在 → 验证 → 根据 role 进入对应页面
  │     ├─ role = configurator → 配置员页面（五.1）
  │     ├─ role = operator     → 运行员页面（五.2）
  │     └─ role = analyst      → 分析员页面（五.3）
  │
  └─ token 不存在 → 登录页
        └─ 输入用户名+密码+选择角色 → AuthServer 验证
              ├─ 配置员 + 非主公机 → "配置员仅限主公机登录"
              └─ 验证通过 → 存入 token+role → 进入对应页面
```

### 配置员 IP 限制

```javascript
// 前端判断
if (role === 'configurator' && window.location.hostname !== 'localhost') {
    alert('配置员仅限主公机登录');
    logout();
}
```

---

## 八、启动流程

```
Step 1: 主公机启动基础设施
   ├─ 启动 Redis (6379)
   ├─ 启动 RabbitMQ (5672)
   └─ 等待就绪

Step 2: 主公机启动核心模块（IDEA 中 Run main()）
   ├─ AuthServer.main()       → :8890（自动创建 admin/admin123 配置员 + operator 运行员演示账号）
   ├─ Controller.main()       → 待命（taskActive=false）
   ├─ Display.main()          → :8888
   └─ Navigator.main()        → 可选

Step 3: 客户端 B—E 启动各自模块（IDEA 中 Run main()）
   ├─ 先启动 TaskCfg/TargetPlanner（如有）
   ├─ 再启动 Navigator + Display
   └─ Car 暂时不启动，等前端操作

Step 4: 各角色登录操作
   ├─ 配置员: 主公机浏览器 localhost:8888 → 登录 → 配置地图参数
   ├─ 运行员: 各自浏览器 localhost:端口 → 登录 → 看到地图和控制按钮
   └─ 分析员: 各自浏览器 localhost:端口 → 登录 → 看到地图和回放面板
```

---

## 九、用户操作流程

### 配置员（主公机，admin/admin123）

```
1. IDEA 中 Run Launcher.main() 或逐个启动各模块 main()
2. 浏览器 localhost:8888 → 登录 → 角色选"配置员"
3. 在控制面板配置地图大小、障碍物密度
4. 群发 "主机就绪，你们登录"
5. 点击【开始】→ 前端布置小车 → 所有小车开始移动
6. 投屏实时演示
```

### 运行员（任意主机登录）

```
1. IDEA 中 Run 各自分配的模块 main()
2. 浏览器 localhost:各自端口 → 登录 → 角色选"运行员"
3. 查看地图实时画面（与主公机同步）
4. 中途可点击【添加小车】→ 新车自动加入探索
5. 可暂停/恢复分配到自己的小车
```

### 分析员（任意主机登录）

```
1. 浏览器 localhost:任意 Display 端口 → 登录 → 角色选"分析员"
2. 查看地图实时画面
3. 使用回放面板查看历史 tick
4. 查看性能图表（探索率曲线、步数统计）
```

### 动态添加小车（运行员操作）

```
任何运行员随时：
1. 点击【添加小车】
2. 系统自动分配唯一 Car ID → 布置到随机空位
3. 所有 Display 画面同步更新
```

---

## 十、画面同步原理

```
Controller tick(500ms)
  │
  ├─ 收集所有车状态 → Redis
  ├─ fanoutPublish("REFRESH_ALL") → RabbitMQ UpdateView Exchange
  │
  └─ ▶ Display(主公机) ──→ 浏览器 8888
      ▶ Display(客户端B) ──→ 浏览器 8889
      ▶ Display(客户端C) ──→ 浏览器 8890
      ▶ Display(客户端D) ──→ 浏览器 8891
      ▶ Display(客户端E) ──→ 浏览器 8892

Fanout Exchange 保证所有人收到相同数据，毫秒级同步
```

---

## 十一、高可用与容错

| 场景 | 影响 | 恢复 |
|------|------|------|
| 某个 Navigator 挂掉 | 无，其他 4 个仍在竞争队列 | 重跑 main() |
| 某辆小车崩溃 | 仅该车 | 前端【添加小车】重新加入 |
| 某台客户端全挂 | 该台车+navigator 停 | 重启模块，其他 4 台不受影响 |
| 客户端中途加入 | 直接启动模块即可，无需重启全局 | 前端添加小车 |
| Display 挂掉 | 仅该台看不到画面 | 重跑 Display.main() |
| Controller 崩溃 | 全局暂停（唯一单点） | 管理员重启 Controller.main() |
| Redis 崩溃 | 全局停 | `CONFIG SET save` 持久化后重启 |
| RabbitMQ 崩溃 | 全局挂起 | 重启 RabbitMQ，各模块自动重连 |
| 认证失败 | 无法登录 | AuthServer 5 次兜底重试 admin |

---

## 十二、防火墙配置（主公机）

```cmd
netsh advfirewall firewall add rule name="BlackBoxAI Redis"     dir=in action=allow protocol=TCP localport=6379
netsh advfirewall firewall add rule name="BlackBoxAI RabbitMQ"  dir=in action=allow protocol=TCP localport=5672
netsh advfirewall firewall add rule name="BlackBoxAI Display"   dir=in action=allow protocol=TCP localport=8888
netsh advfirewall firewall add rule name="BlackBoxAI Auth"      dir=in action=allow protocol=TCP localport=8890
```

## 附录：实施记录（2026-06-20）

以下功能已实施：

| 功能 | 状态 |
|------|------|
| 前端 SHA-256 密码传输 | ✅ |
| 后端两层 SHA-256(salt+transHash) | ✅ |
| WebSocket Token 认证 (AUTH/AUTH_OK) | ✅ |
| 后端权限校验 (role-based) | ✅ |
| 配置员 IP 限制 (仅 localhost) | ✅ |
| 配置员完整面板（全局控制/用户管理/障碍物编辑） | ✅ |
| 运行员面板（我的小车/personal 暂停/全局暂停锁定） | ✅ |
| 分析员面板（回放控件/折线图/统计） | ✅ |
| 全局暂停 + 运行员个人暂停 | ✅ |
| Car owner 归属 (machineId) | ✅ |
| 所有模块支持 --redis-host/port --mq-host/port | ✅ |
| Display --machine + /api/config 端点 | ✅ |
| ReplayServer 批量快照接口 + 端口 8893 | ✅ |
| Launcher 单机调试模式 | ✅ |

各机器启动命令参见第四章。
